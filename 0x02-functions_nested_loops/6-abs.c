#include "main.h"

/**
 * _abs - Computess the absolute value of an integer.
 * @n: The integers to be computed.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
